# podman run --rm -v "$PWD:$PWD:z" -w "$PWD"  registry.redhat.io/ubi8/ubi-minimal cp -r /etc/yum.repos.d/. .
# https://github.com/konflux-ci/rpm-lockfile-prototype/blob/main/README.md#running-in-a-container
# podman run --rm -v ${PWD}:/tmp localhost/rpm-lockfile-prototype:latest --outfile=/tmp/rpms.lock.yaml /tmp/rpms.in.yaml

contentOrigin:
  repofiles:
    - ubi.repo
packages:
  - ca-certificates
  - openssl
arches:
  - x86_64
  - aarch64
  - s390x
  - ppc64le
context:
  containerfile:
    file: Dockerfile