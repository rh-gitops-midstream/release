# Argo CD GitOps Promoter Extension Installer

### Note: Extract `extension-promoter.js` from <https://github.com/argoproj-labs/gitops-promoter/releases> tar for when we want to update the extension

Currently the version we are shipping is the one from v0.35.0.

### TODO: same as the rollouts extension build the extension from the source in Dockerfile instead of using prebuilt js

This uses the same install script that is located inside of the rollouts extension.
