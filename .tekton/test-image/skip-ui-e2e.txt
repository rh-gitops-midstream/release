# Tests to skip when running UI E2E tests on EaaS HyperShift clusters.
# One regex pattern per line. Blank lines and lines starting with # are ignored.
# These are passed to playwright --grep-invert as a combined '|' regex.
